const btn = document.querySelector('.btn');
    btn .addEventListener('click', () => {
        const result = document.querySelector('.conf');
        result.innerText = `Your WebApp is ready for Action.`;
    });